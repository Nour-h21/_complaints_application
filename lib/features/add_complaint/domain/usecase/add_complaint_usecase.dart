import 'package:flutter/material.dart';

class AddComplaintUsecase extends StatelessWidget {
  const AddComplaintUsecase({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}