import 'package:flutter/material.dart';

class AddComplaintModel extends StatelessWidget {
  const AddComplaintModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}