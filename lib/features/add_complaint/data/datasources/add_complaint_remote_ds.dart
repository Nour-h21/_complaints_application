import 'package:flutter/material.dart';

class AddComplaintRemoteDs extends StatelessWidget {
  const AddComplaintRemoteDs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}