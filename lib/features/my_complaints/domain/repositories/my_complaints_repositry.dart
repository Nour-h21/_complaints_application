import 'package:flutter/material.dart';

class MyComplaintsRepositry extends StatelessWidget {
  const MyComplaintsRepositry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}