import 'package:flutter/material.dart';

class MyComplaintsUsecase extends StatelessWidget {
  const MyComplaintsUsecase({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}