import 'package:flutter/material.dart';

class LoginRepositryImpl extends StatelessWidget {
  const LoginRepositryImpl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}