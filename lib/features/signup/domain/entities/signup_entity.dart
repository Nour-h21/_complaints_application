import 'package:flutter/material.dart';

class SignupEntity extends StatelessWidget {
  const SignupEntity({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}