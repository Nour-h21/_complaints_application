import 'package:flutter/material.dart';

class SignupRepositoryImpl extends StatelessWidget {
  const SignupRepositoryImpl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}