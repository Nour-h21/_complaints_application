import 'package:flutter/material.dart';

class VerificationCodeRepository extends StatelessWidget {
  const VerificationCodeRepository({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}