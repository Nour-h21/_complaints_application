import 'package:flutter/material.dart';

class VerificationCodeUsecase extends StatelessWidget {
  const VerificationCodeUsecase({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}