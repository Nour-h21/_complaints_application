import 'package:flutter/material.dart';

class VerificationCodeEntity extends StatelessWidget {
  const VerificationCodeEntity({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}