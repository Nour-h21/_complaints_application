import 'package:flutter/material.dart';

class VerificationCodeRepositoryImpl extends StatelessWidget {
  const VerificationCodeRepositoryImpl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}