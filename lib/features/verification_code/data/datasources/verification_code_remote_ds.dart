import 'package:flutter/material.dart';

class VerificationCodeRemoteDs extends StatelessWidget {
  const VerificationCodeRemoteDs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}